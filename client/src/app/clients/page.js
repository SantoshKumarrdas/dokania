import React from 'react';
import ClientsPage from '@/components/clients/ClientsPage';

export const metadata = {
    title: 'Our Clients - Dokania Tech Solutions',
    description: 'Discover our trusted clients and success stories. We serve leading companies across HVAC, EPS, automotive, and industrial sectors with reliable solutions.',
};

const Clients = () => {
    return <ClientsPage />;
};

export default Clients;
